from setuptools import setup, find_packages

setup(
    name='adt_examples',  # Package name
    version='1.0',  # Package version
    packages=find_packages(),  # Automatically find all packages
    install_requires=[],
)
